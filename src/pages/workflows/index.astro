---
import Layout from '../../layouts/Layout.astro';
import Header from '../../components/Header.jsx';
import Footer from '../../components/Footer.jsx';
import { workflowData } from '../../services/workflowData';
import { Settings, ArrowRight, Gauge, CheckCircle } from 'lucide-react';

const workflows = Object.entries(workflowData).map(([slug, data]) => ({
  slug,
  ...data
}));
---

<Layout title="Surgical & Diagnostic Workflows | Precision Ortho Process Hub">
  

  <section class="bg-primary text-white py-20 border-b-4 border-secondary">
    <div class="container">
      <h1 class="display-xl">Surgical <span class="text-secondary">Workflows</span></h1>
      <p class="hero-subtext max-w-3xl mt-6">
        Transparency in the process. Our surgical and diagnostic workflows represent the meticulous sequencing 
        of technology and clinical expertise to ensure predictable, elite-grade outcomes.
      </p>
    </div>
  </section>

  <section class="py-20 bg-gray-50">
    <div class="container">
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        {workflows.map((flow) => (
          <div class="bg-zinc-card rounded-xl overflow-hidden shadow-sm hover:shadow-md transition-shadow flex flex-col">
            <div class="p-8 flex-grow">
              <div class="flex items-center gap-3 text-secondary mb-4">
                <Settings size={24} />
                <span class="text-sm font-bold tracking-widest uppercase">Clinical Workflow</span>
              </div>
              <h3 class="text-2xl font-bold text-white mb-2">{flow.h1}</h3>
              <p class="text-sm text-secondary font-bold mb-4">Goal: {flow.clinicalObjective.split('.')[0]}.</p>
              
              <div class="space-y-4 mb-6">
                {flow.protocolSteps.slice(0, 3).map(step => (
                  <div class="flex items-start gap-3">
                    <div class="w-6 h-6 rounded-full bg-secondary/10 text-secondary flex items-center justify-center flex-shrink-0 mt-0.5">
                      <span class="text-[10px] font-black">?</span>
                    </div>
                    <div>
                      <div class="text-xs font-bold text-white uppercase">{step.step}</div>
                      <div class="text-sm text-gray-400 line-clamp-1">{step.action}</div>
                    </div>
                  </div>
                ))}
              </div>
            </div>
            
            <div class="px-8 py-6 bg-white/5 border-t border-white/10">
              <a href={`/workflows/${flow.slug}`} class="flex items-center justify-between font-bold text-white hover:text-secondary transition-colors">
                Examine Workflow <ArrowRight size={20} />
              </a>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <Footer />
</Layout>

<style>
  .hero-subtext {
    font-size: 1.25rem;
    color: #E0E1DD;
    line-height: 1.6;
  }
  .line-clamp-1 {
    display: -webkit-box;
    -webkit-line-clamp: 1;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
</style>
